//  Using Ternary Operator
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int num =-9;
    
  //Conditions to check if the number is negative or positive
  
    if(num==0)
    cout<<"Zero"; else(num<0) ? cout<<"Negative": cout<<"positive";
     //(Condition ) ? ( if True : Action) : ( if False : Action) ;
    return 0;
}
    
    
//Output 
//Negative